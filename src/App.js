import React from "react";
import { MainPage, SectionMore, SectionCarousel } from "components/Components";

const App = () => {
  return (
    <>
      <MainPage />
      <SectionMore />
      <SectionCarousel />
    </>
  );
};

export default App;
